# Only required for the optional "Archive" persistence feature.
# Everything else (scanning, filtering, import) runs without a database.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/app_db
